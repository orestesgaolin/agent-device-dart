# agent-device → Dart Port: Plan

Source: `agent-device/` (TypeScript, ~116K LOC, Node >=22).
Target: a Dart package usable as (a) a CLI (`agent-device` / `ad`) and (b) a library consumable by other Dart packages. Same command surface, same `.ad` replay format, compatible wire protocol where practical.

Keep this document up to date — edit phases, add decisions/risks, check off work as it lands.

---

## 1. Goals & Non-Goals

Goals
- Match the public CLI surface of `agent-device` — same subcommands, flags, exit codes, JSON shape.
- Run `.ad` replay scripts with the same semantics (metadata `context` header, line-per-action grammar).
- Expose a Dart library API analogous to the Node `createAgentDevice` / `createAgentDeviceClient` / `commands` / `backend` surface so other Dart packages can drive devices programmatically.
- Preserve daemon session model (open → interact → close) and replay/heal semantics.
- Keep parity on core platforms: iOS (simulator + device), Android, macOS, Linux. tvOS treated as an iOS variant.

Non-goals (initial)
- Rewriting the Swift iOS XCUITest runner (`ios-runner/`) or the Swift `macos-helper` — these are already standalone subprocesses invoked by the Node daemon; the Dart port reuses those binaries as-is.
- Rewriting the Python AT-SPI bridge (`src/platforms/linux/atspi-dump.py`) — invoked as a subprocess; ship and call it unchanged.
- 1:1 byte-compatibility of the daemon wire format with the Node daemon. We pick a compatible JSON RPC shape but do not guarantee the Dart daemon and Node CLI interoperate.
- Website / marketing / skill docs. The `skills/` tree is interesting (agent guidance) but not required for feature parity; port later if requested.

---

## 2. Current Architecture (source of the port)

One paragraph per layer. File paths are under `agent-device/src/` unless noted.

- **CLI** (`bin.ts`, `cli.ts`, `cli/commands/router.ts`, `utils/args.ts`, `utils/command-schema.ts`, `utils/cli-options.ts`): argv parse → resolve flags → optionally materialize remote config → send to daemon (auto-spawn if missing) → format JSON or human output.
- **Daemon client transport** (`daemon-client.ts`): HTTP and Unix/TCP socket to local daemon; lock-file `~/.agent-device/daemon.lock`, info file `~/.agent-device/daemon.json` (port, token, signature). Auto-spawns `node bin/agent-device.mjs --daemon` on demand.
- **Daemon** (`daemon.ts`, `daemon/http-server.ts`, `daemon/transport.ts`, `daemon/server-lifecycle.ts`, `daemon/session-store.ts`, `daemon/request-router.ts`): dual-mode (HTTP or net socket) JSON RPC. Sessions persisted to `~/.agent-device/sessions/<name>.json`. ~78 handler files under `daemon/handlers/` decomposed by concern (session lifecycle, snapshot, interaction, find, record-trace, replay, perf, observability).
- **Runtime / in-process API** (`runtime.ts`, `core/dispatch.ts`, `core/capabilities.ts`, `core/interactors.ts`): the same command set, executed in-process against a `Backend`, bypassing daemon. Used by SDK consumers and by the daemon itself.
- **Backend abstraction** (`backend.ts`): ~40 methods (snapshot, screenshot, tap, swipe, scroll, type, fill, install, keyboard, clipboard, logs, network, perf, recording, alert, shell …). Platform modules under `platforms/{ios,android,linux}` + macOS helpers under `platforms/ios/macos-*.ts` implement it.
- **Client library** (`client.ts`, `client-*.ts`, `index.ts`): typed facade over the daemon transport (or in-process runtime). Public SDK; sub-path exports: `./commands`, `./backend`, `./io`, `./artifacts`, `./metro`, `./remote-config`, `./install-source`, `./android-apps`, `./contracts`, `./selectors`, `./finders`.
- **Selectors / snapshots** (`daemon/selectors*.ts`, `utils/snapshot*.ts`, `utils/selector-*.ts`): snapshot tree model, selector DSL (parse/resolve/match/heal), snapshot diff, visibility, ref-vs-selector rules.
- **`.ad` replay** (`daemon/handlers/session-replay*.ts`, `daemon/handlers/session-test*.ts`, `daemon/handlers/session-open-script.ts`): text-based script grammar — `context platform=<p> timeout=<ms> retries=<n>` metadata, `#` comments, then one action per line (`open ...`, `snapshot -i`, `click "selector"`, `tap x y`, etc.). Runner supports per-test retries, artifacts under `.agent-device/test-artifacts/`.
- **Native binaries** (NOT in `src/`):
  - `ios-runner/AgentDeviceRunner/` — Swift Xcode project, XCUITest-based TCP server for gesture/snapshot on iOS/tvOS/macOS simulator and device.
  - `macos-helper/` — Swift Package providing `agent-device-macos-helper` binary for macOS desktop accessibility snapshots.
  - `src/platforms/linux/atspi-dump.py` — Python AT-SPI introspection subprocess.
- **External tools spawned** (`utils/exec.ts` wraps): `adb`, `xcrun simctl`, `xcrun devicectl`, `swift`, `xcodebuild`, `python3`, `ffmpeg`-style recording via platform tools.
- **NPM deps**: only `fast-xml-parser` (plist/XML) + `pngjs` (screenshot diff pixel ops). Everything else is Node built-ins.

Client commands (canonical list — `src/client-command-registry.ts`):
`alert, appstate, app-switcher, apps, back, batch, boot, click, clipboard, devices, diff, fill, find, focus, get, home, is, keyboard, logs, longpress, network, perf, pinch, press, push, record, replay, rotate, scroll, screenshot, settings, snapshot, swipe, test, trace, trigger-app-event, type, wait`.

---

## 3. Target Dart Layout

Repo: `agent-device-dart/` (sibling of `agent-device/` source). Will be a pub workspace.

```
agent-device-dart/
  pubspec.yaml                     # workspace root (or single package for v0)
  packages/
    agent_device/                  # main library + CLI (`dart pub global activate`)
      bin/
        agent_device.dart          # CLI entry (argv -> runCli)
      lib/
        agent_device.dart          # public export (mirrors src/index.ts)
        src/
          cli/                     # CLI parsing, help, routing  (mirrors src/cli.ts, utils/args.ts)
          runtime/                 # in-process runtime  (mirrors src/runtime.ts, core/)
          client/                  # daemon client facade (mirrors src/client.ts)
          backend/                 # Backend abstract class (mirrors src/backend.ts)
          commands/                # command catalog + implementations (mirrors src/commands/)
          daemon/                  # daemon process + handlers (mirrors src/daemon/)
          platforms/
            ios/                   # simctl/devicectl/runner-client
            android/               # adb + UIAutomator XML
            macos/                 # macos-helper subprocess wrapper
            linux/                 # atspi-dump.py wrapper
          selectors/               # selector DSL  (mirrors src/daemon/selectors*.ts + src/utils/selector-*.ts)
          snapshot/                # snapshot model + diff + processing
          replay/                  # .ad parser + test runner
          utils/                   # exec, errors, diagnostics, png, xml, etc.
      test/
      native/                      # bundled Swift/Python assets (copied from agent-device/)
    agent_device_testing/          # optional: conformance + fixtures (mirrors testing/)
```

Key packaging decisions to revisit:
- Ship `ios-runner/`, `macos-helper/`, `atspi-dump.py` in `packages/agent_device/native/` so the CLI can locate them by a path relative to the Dart entry script.
- Daemon process: same Dart binary re-entered with `--daemon` (just like Node's `bin.ts`).

---

## 4. Dependency Mapping (Node → Dart)

| Node / npm | Dart equivalent | Notes |
|---|---|---|
| `node:fs`, `node:fs/promises` | `dart:io` | Direct. |
| `node:path` | `package:path` | Direct. |
| `node:child_process` spawn | `dart:io` `Process.start` / `Process.run` | Streaming stdout/stderr fine. Need helper parity with `utils/exec.ts`. |
| `node:http` server | `package:shelf` + `package:shelf_router` | Tiny surface — daemon RPC is one POST handler. |
| `node:http` client | `package:http` or `dart:io` `HttpClient` | `HttpClient` keeps deps minimal. |
| `node:net` (Unix sockets / TCP) | `dart:io` `ServerSocket` / `RawSocket` | Unix sockets via `InternetAddress.unix`. |
| `node:crypto` | `dart:math` + `package:crypto` | For tokens. |
| `node:url` | `dart:core` `Uri` | Direct. |
| `node:stream` | `dart:async` `Stream` | Direct conceptual mapping. |
| `node:events` EventEmitter | `StreamController` | Direct. |
| `pngjs` | `package:image` | Pixel diff + region rendering. Consider FFI to libpng only if `image` too slow. |
| `fast-xml-parser` | `package:xml` | Plist parsing may need small helper layered on top. |
| `AbortSignal.timeout` | `Future.timeout` / `CancelableOperation` | |
| `JSON.stringify/parse` | `dart:convert` `jsonEncode/jsonDecode` | |
| Node `process.argv/env/exit` | `Platform.environment`, `Platform.executableArguments`, `exit()` | |
| `pathToFileURL` | `Uri.file` | |
| Node lock/single-instance via flock-like | `File.openSync(mode: WRITE)` + advisory lock via `RandomAccessFile.lock` | `dart:io` supports `lock()`. |

Dart-only considerations:
- Isolates for CPU-heavy PNG diff if it becomes a bottleneck.
- `dart:ffi` deferred — only needed if we replace `macos-helper` with in-process accessibility calls (not planned).

---

## 5. Phased Delivery

Each phase should compile, pass its own tests, and leave the repo in a green state. Phase ordering reflects dependency — don't start phase N+1 before N is landed.

### Phase 0 — Scaffolding & tooling (0.5 day)
- Create `agent-device-dart/packages/agent_device` with `pubspec.yaml`, Dart SDK constraint, analyzer/lints config.
- Wire `dart format`, `dart analyze`, `dart test` via a top-level `Makefile` / `justfile` / `melos.yaml`.
- CI skeleton (GH Actions).
- Commit.

### Phase 1 — Pure-Dart foundations (3–5 days)
No process spawning, no network. Types + parsing + formatters.
- Port `utils/errors.ts` → `lib/src/utils/errors.dart` (`AppError`, normalization, codes).
- Port `utils/snapshot.ts`, `utils/snapshot-tree.ts`, `utils/snapshot-visibility.ts`, `utils/snapshot-lines.ts`, `utils/snapshot-processing.ts`, `utils/snapshot-diff.ts` → `lib/src/snapshot/*`.
- Port `utils/selector-*.ts` + `daemon/selectors*.ts` (parse/match/resolve, no IO side) → `lib/src/selectors/*`.
- Port `.ad` parser + writer (`daemon/handlers/session-replay-script.ts`, `session-open-script.ts`) → `lib/src/replay/script.dart`.
- ~~Port command schema + arg parser~~ **Skipped.** `utils/command-schema.ts` (1589 LOC) + `utils/args.ts` + `utils/cli-option-schema.ts` + `utils/cli-options.ts` exist to feed a custom TS arg parser. In Dart we use `package:args` with per-command `Command` subclasses that declare flags inline — that work happens in Phase 5 as each CLI command is ported. The shared universal flags (`--session`, `--platform`, `--json`, `--verbose` / `--debug`, `--state-dir`, etc.) live on a base `Command` class. Net deleted: ~2100 LOC of data-shaped code.
- Port `commands/index.ts` catalog metadata → `lib/src/commands/catalog.dart`. Downstream SDK consumers rely on `commandCatalog` for docs/LSP; this is separate from CLI arg parsing and still needs a small port (data only, no logic).
- Snapshot test fixtures: load a handful of real `.ad` scripts and snapshot JSON samples; assert equivalence with Node outputs.
- **Subagent strategy**: delegate each file's port to a haiku agent in isolation (`isolation: worktree`) with the source file + target path; review and merge.

### Phase 2 — Exec + platform primitives (3 days)
- Port `utils/exec.ts` (`runCmd`/`runCmdSync`) → `lib/src/utils/exec.dart`. This is a hard rule from `AGENTS.md` — everything spawns through it.
- Port `utils/png.ts` → `lib/src/utils/png.dart` using `package:image`.
- Port `utils/redaction.ts`, `utils/retry.ts`, `utils/path-resolution.ts`, `utils/timeouts.ts`, `utils/output.ts`, `utils/version.ts`.
- Port `utils/diagnostics.ts` (scopes, emit, flush) → `lib/src/utils/diagnostics.dart`.

### Phase 3 — Backend interface + Android first (5–7 days)
Pick Android as the first real backend (simplest external dep: just `adb`).
- First, fill the verifier-flagged exec gaps: `runCmdDetached` returns `Future<Process>` (caller awaits the spawn, receives a live handle); `runCmdBackground` returns a `({Process process, Future<RunCmdResult> wait})` record matching the TS `ExecBackgroundResult` shape. **Decision (2026-04-23)**: `runCmdDetached` returns `Future<Process>`, not `Future<int>` — closer to TS usage sites and gives the caller full lifecycle control.
- Define `abstract class Backend` matching `src/backend.ts`.
- Port `platforms/android/*`: `adb.ts`, `ui-hierarchy.ts` (XML → SnapshotNode), `devices.ts`, `app-lifecycle.ts`, `input-actions.ts`, `screenshot.ts`, `snapshot.ts`, `install-artifact.ts`, `manifest.ts`, `app-parsers.ts`, `settings.ts`, `perf.ts`, `notifications.ts`, `open-target.ts`, `scroll-hints.ts`, `sdk.ts`, `device-input-state.ts`. ~4050 TS LOC; split into three waves:
  - **Wave A** (foundations): `adb`, `sdk`, `devices`, `manifest`, `app-parsers`, `install-artifact` — standalone utilities.
  - **Wave B** (snapshot layer): `ui-hierarchy`, `snapshot`, `screenshot`, `scroll-hints`.
  - **Wave C** (actions + lifecycle + assembly): `input-actions`, `app-lifecycle`, `device-input-state`, `notifications`, `open-target`, `perf`, `settings`, `index` (the `AndroidBackend` class that implements `Backend`).
- **Integration testing**: mirror the TS `android.yml` workflow — `reactivecircus/android-emulator-runner@v2` on ubuntu-latest (pixel_7, api 36, google_apis_playstore), run `.ad` replay scripts (`test/integration/replays/android/*.ad`) through the Dart CLI. Keep local unit tests pure (parsing ADB output strings, no real subprocess) so `dart test` stays fast; integration tests gate on `AGENT_DEVICE_ANDROID_IT=1` for local dev. CI workflow added in the last step of this phase, after there's something real to test.

### Phase 4 — Runtime + in-process SDK (3 days)

**Design deviation from TS (decision 2026-04-23):** the TS `bindCommands` pattern generates ~40 methods on the runtime via dynamic TypeScript. It doesn't port cleanly to Dart and the resulting `Map<String, Function>` wouldn't be type-safe for SDK consumers. Instead, build a Dart-idiomatic `AgentDevice` class with typed method signatures wrapping the `Backend`. This is the shape other Dart packages will import. The CLI (Phase 5) and `.ad` replay runner (Phase 10) are the stable user-facing contracts; the programmatic API gets a Dart-native shape.

Scope:
- Port `runtime-contract.ts` types (`CommandSessionRecord`, `CommandSessionStore`, `CommandPolicy`, etc.) → `lib/src/runtime/contract.dart`.
- Port memory session store → `lib/src/runtime/session_store.dart`.
- Port minimal device resolution (pick first matching device via `Backend.listDevices`) → `lib/src/runtime/device_resolver.dart`. Full `resolveTargetDevice` with CLI flags lands in Phase 5; for now, programmatic callers pass a simple filter.
- New file (Dart-only): `lib/src/runtime/agent_device.dart` — `AgentDevice` class with typed methods (`open`, `snapshot`, `tap`, `fill`, `openApp`, etc.) that build `BackendCommandContext` with `deviceSerial` resolved from session state, dispatch to `backend.<method>`, and update session state on mutation.
- Defer until needed by Phase 5: `core/dispatch.ts` (906 LOC — this ties together CLI flags, device resolution, batch/series, and replay healing); `core/batch.ts`; `core/interactors.ts` (selector→point resolution — can stay on the Backend side for now via `snapshot+find` rather than a separate interactor registry); `commands/index.ts` catalog metadata.
- Smoke test: rewrite `android_live_test.dart` to use `AgentDevice` instead of raw `AndroidBackend` calls. Proves the programmatic API works end-to-end on a real device.

### Phase 5 — CLI (no daemon yet) (3 days)
- Wire `bin/agent_device.dart` → parse argv → dispatch locally through the in-process runtime (single-shot execution, sessions in-memory for now).
- Port `utils/output.ts` (JSON + human formatters), `cli/commands/*.ts`.
- Port update-notifier as no-op stub initially (`utils/update-check.ts`).
- Produce a usable CLI for Android, even without the daemon. All of `snapshot`, `click`, `fill`, `type`, `scroll`, `back`, `home`, `screenshot`, `open`, `close` should work.
- Parity test: run the same `.ad` script in Node and Dart; compare JSON output.

### Phase 6 — Daemon + transport (4 days)
- Port `daemon-client.ts`, `daemon.ts`, `daemon/http-server.ts`, `daemon/transport.ts`, `daemon/server-lifecycle.ts`, `daemon/config.ts`, `daemon/request-router.ts`, `daemon/response.ts`.
- Implement lockfile + info file with the same filenames under `~/.agent-device/` so both Node and Dart daemons are mutually exclusive (not interoperable, just non-colliding — pick a different port or fail fast if Node daemon detected).
- Port the session-lifecycle handlers under `daemon/handlers/session*.ts`.
- Now `agent-device open` / `close` / `snapshot` all round-trip through a Dart daemon process.

### Phase 7 — Interaction + selector handlers (4 days)
- Port `daemon/handlers/interaction*.ts`, `find.ts`, `snapshot*.ts`, `is-predicates.ts`, `action-utils.ts`, `selector*.ts` (the handler side — parse/resolve/heal).
- Replay heal: `session-replay-heal.ts`.
- Batch runner: `handlers/session-batch.ts`.

### Phase 8 — iOS backend + runner bridge (6–8 days)
- Port `platforms/ios/` (simctl, devicectl, plist, xml, apps, devices, screenshot, perf).
- Port `platforms/ios/runner-client.ts`, `runner-transport.ts`, `runner-contract.ts`, `runner-session.ts`, `runner-xctestrun.ts` — TCP client to existing Swift `ios-runner/` binary.
- Reuse the existing Swift project as-is (build via `xcodebuild`); Dart just drives it.
- `ensure-simulator` command.

### Phase 9 — macOS + Linux (3 days)
- macOS: wrap `macos-helper` binary (Swift); port `macos-apps.ts`, `macos-helper.ts`.
- Linux: wrap `atspi-dump.py`; port `platforms/linux/*`.

### Phase 10 — Replay test runner, record/trace, observability (4 days)
- `daemon/handlers/session-test*.ts`, `session-replay*.ts`, `record-trace*.ts`, `app-log*.ts`, `network-log.ts`, `recording-*.ts`, `upload.ts`, `artifact-*.ts`.
- `test` command: glob of `.ad` files with retries, per-test artifacts under `.agent-device/test-artifacts/`.

### Phase 11 — Metro + remote-config + connection (3 days)
- `metro*.ts`, `remote-config*.ts`, `remote-connection-state.ts`, `cli/commands/connection*.ts`, `install-source*.ts`. React-Native specific; lower priority but needed for parity.

### Phase 12 — Polish, docs, release (2 days)
- Update-notifier, `--version`, `--help`, shell completions.
- Cross-check CLI output byte-for-byte with Node on a suite of `.ad` scripts (per-platform).
- Publish to pub.dev (internal dry-run first).
- Compiled binaries via `dart compile exe` per-OS.

Rough total: **~6–8 engineering weeks** for single-developer parity port; realistically longer given surface area. Plan to release Android + CLI + daemon first (through Phase 7) as an MVP, then stabilize with iOS.

---

## 6. Subagent Workflow

The user asked for subagents (cheaper models) to chip away at discrete tasks. Template:

```
Agent({
  description: "Port <file> to Dart",
  subagent_type: "general-purpose",
  model: "haiku",                        # cost-control; bump to sonnet if complex
  isolation: "worktree",                 # safer for parallel tasks
  prompt: """
    Task: port `agent-device/src/<path>.ts` to
    `agent-device-dart/packages/agent_device/lib/src/<dest>.dart`.

    Source file: <path>
    Dependencies already ported: <list module paths>
    Target API: preserve exported names (snake_case → dart style).
    Do NOT port tests; write a thin smoke test that compiles.
    Keep semantics identical. Flag open questions in a `// TODO(port):` comment.

    Run: `dart analyze packages/agent_device` and `dart format` before finishing.
    Report: new files created, any semantic ambiguity, and tests added.
  """,
})
```

Rules for subagent dispatch:
- One file (or one tight module group) per subagent. No porting of `daemon/handlers/` en masse.
- **Always pass the list of already-ported dependencies** so the agent imports the Dart versions, not re-ports them.
- Verify each subagent's output with `Read` + `dart analyze` before accepting; don't trust the summary.
- Use `isolation: "worktree"` for any port that touches more than one file — easier rollback.
- Use `model: "haiku"` by default for mechanical translations; escalate to `sonnet` for logic-heavy files (selector engine, replay heal, dispatch).

Parallelism: files within the same phase whose dep graphs don't overlap can be ported in parallel — e.g. phase 1 utilities (errors, snapshot-tree, png) are independent.

---

## 7. Risks & Open Questions

- **CLI flag parity**: `utils/command-schema.ts` encodes hundreds of flags. Hand-porting is error-prone; consider a code-gen step that reads the TS AST (via `tsc --emit` to JSON or a simple grep). Decide before Phase 1.
- **Selector engine correctness**: the selector DSL + healing logic is subtle. Plan a fixture-based equivalence suite: run the Node selector engine on a corpus of snapshots and selectors, capture results as JSON, replay the same corpus against Dart.
- **PNG diff performance**: `package:image` is pure Dart and may be 5–10× slower than `pngjs`. Budget a benchmark; fall back to FFI if needed.
- **Daemon interop with Node**: decided NOT to interop. The two implementations detect each other via the shared lockfile and refuse to start concurrently.
- **iOS runner protocol drift**: the Swift `ios-runner/` wire protocol is not versioned. Pin to a specific `ios-runner/` build and document it in the port. Rebuild Swift only when the Dart port supports it.
- **Native runner reuse confirmed (2026-04-23)**: the Swift `ios-runner/` project and `macos-helper/` binary are reused as-is via subprocess — no Dart rewrite, no `dart:ffi` bridge. Keep the platform seam thin so a future FFI-backed replacement is possible but don't extract platform-agnostic helpers speculatively.
- **Skills / docs**: the `skills/` directory (agent-operating guidance) ships with the npm package. Decide whether to copy them verbatim into the Dart package or leave upstream.
- **Test execution model**: Node version uses `vitest` for unit + `node --test` for integration. Dart uses `package:test`. Need fixtures + an integration-gated harness; mirror `CONTRIBUTING.md` env vars (`ANDROID_SERIAL`, `IOS_UDID`).

---

## 8. Status / Changelog

- **2026-04-23**: Plan drafted.
- **2026-04-23**: Phase 0 done. Pub workspace at repo root, package at `packages/agent_device` (Dart SDK ^3.11, `args` / `path` / `crypto` / `http` / `shelf` / `shelf_router` / `xml` / `image` / `meta`, dev: `lints` / `test`). Strict analyzer (`strict-casts` / `strict-inference` / `strict-raw-types`). CLI stub at `bin/agent_device.dart` with `--version`; smoke test green; `dart analyze` clean; `dart format` clean. Makefile (`get` / `analyze` / `format` / `test` / `check`) + GH Actions CI on macos-latest.
- **2026-04-23**: Phase 1 partial — ported `utils/errors.ts` + `utils/redaction.ts` (hand) and the 6-file `utils/snapshot*.ts` group (haiku subagent, inline; worktree isolation rejected so review-by-diff). 51 tests passing; analyze clean. Open TODOs: `snapshot/visibility.dart` stubs mobile-surface semantics pending port of `utils/mobile-snapshot-semantics.ts`; `processing.dart::extractNodeReadText` aliases `extractNodeText` pending `text-surface.ts` port.
- **2026-04-23**: Phase 1 continued — selectors (7 files, ~906 Dart LOC) and `.ad` replay parser/serializer (4 files, ~1055 Dart LOC) ported by two haiku subagents. Also ported minimal `PlatformSelector` enum under `lib/src/platforms/`. Replay subagent added a small `SessionRuntimeHints` type (real type from `daemon/types.ts`, verified). 169 tests passing; analyze clean. Decision recorded above: no port of `command-schema.ts` / `args.ts` / `cli-option-*.ts` — switch to `package:args` `Command` classes per command in Phase 5. Phase 1 now substantively complete except for the small `commands/index.ts` catalog metadata, which can wait until Phase 4 (runtime) since it depends on types defined there.
- **2026-04-23**: Phase 2 — exec/png/diagnostics/retry/path_resolution/timeouts ported by two parallel `feature-port-haiku` subagents (~1,480 Dart LOC), then audited by `feature-port-verifier` (sonnet). Verifier caught two real bugs that slipped past analyze/tests: exec null-byte guard used digit-zero `'0'` rather than NUL `\x00`; timeout used SIGTERM instead of SIGKILL. Both fixed in `fe3a0ff` with regression tests for the null-byte case. 272 tests passing. Key Node→Dart divergences: `AsyncLocalStorage` → Dart `Zone` values; `AbortSignal` → custom `CancelToken`; `Process.runSync` has no timeout param so `runCmdSync` silently ignores `timeoutMs` (documented); `runCmdBackground` / `runCmdDetached` stubbed as `NOT_IMPLEMENTED` because Dart's `Process.start` is async — Phase 3 (Android) will force real implementations (plan notes below). `output.ts` deferred — depends on the un-ported `screenshot-diff*.ts` and `mobile-snapshot-semantics.ts` chains.
- **2026-04-23**: Phase 8A done — minimum-viable iOS backend via `simctl`. Commit `e7eed5b`. Five Backend methods wired on `IosBackend`: `listDevices` (via `xcrun simctl list devices -j`), `captureScreenshot` (via `simctl io <udid> screenshot`), `openApp` / `closeApp` / `listApps` (the last pipes `simctl listapps` through `plutil -convert json` since the raw output is NeXTSTEP plist format), plus a best-effort `getAppState` via `launchctl list`. Snapshot, tap, fill, selector-targeted commands stay on the Backend default (`UNSUPPORTED_OPERATION`) until Phase 8B ports the XCUITest runner bridge (runner-transport / runner-session / runner-xctestrun — ~3K LOC plus Swift build orchestration from `ios-runner/`). CLI wires `--platform ios` / `--platform apple` → `IosBackend`. Validated end-to-end against a booted iPhone 17 / iOS 26.2 simulator: 22 simulators listed, 3.1 MB PNG captured, 33 apps listed (including `com.apple.mobilesafari`), Safari open + close round-trip visible on screen. 5 new live tests gated on `AGENT_DEVICE_IOS_LIVE=1` that auto-detect the booted simulator; 542 unit tests unchanged; `dart analyze` clean. Deferred Phase 8B: XCUITest runner build + TCP transport + snapshot/tap/fill, plus `ensureSimulator` + `devicectl` for physical-device install/launch, plus macOS helper (Phase 9 territory).
- **2026-04-23**: Phase 7 done — selector/ref targeting on `AgentDevice` and CLI. Commit `de2a6cf`. Extends the programmatic façade with a sealed `InteractionTarget` hierarchy (`PointTarget` | `RefTarget` | `SelectorTarget`), a `.parseArgs(List<String>)` auto-detecting x-y coords vs `@ref` vs selector expression, plus target-based `tapTarget` / `fillTarget` / `focusTarget` / `longPressTarget` wrappers and four new methods: `find(text)` (substring search over label/value/identifier), `getAttr(attr, target)`, `isPredicate(predicate, target, {expectedText})`, `wait(predicate, target, {timeout, pollInterval})`. CLI gains `press`, `find`, `get`, `is`, `wait` — `is` and `wait` exit 1 on FAIL for shell-pipeline composition. Two real bugs surfaced and fixed: `AndroidBackend.captureSnapshot` returned `RawSnapshotNode`s without calling `attachRefs`, so Phase 1's `findNodeByRef` would never match (threaded `attachRefs` in); `selectors/is_predicates.dart::evaluateIsPredicate` had no `case 'exists':` branch in its switch, making `is exists` always return `pass: false` regardless of match state (fixed + regression test). Test contract also corrected: TS selector syntax is whitespace-AND, not `&&`. Deferred: per-interaction selector *healing* across failed replays (needs Phase 10 replay runner context); disambiguation heuristics for ambiguous matches are already in `resolveSelectorChain` and wired through `_resolveNode`. 11 new unit tests + 9 new live CLI tests on the Pixel 9 Pro emulator (find, get identifier, is exists/visible/editable/text, press @ref, press id=..., wait exists success + timeout). Totals: 542 unit + 14 programmatic/xinv/selector/bbox live + 23 CLI blackbox live = 579 tests green, `dart analyze` clean.
- **2026-04-23**: Phase 6A done — disk-backed session store unlocks cross-invocation state sharing in the CLI. Commit `48279ed`. Design note recorded: the TS `src/daemon/session-store.ts` is actually an in-memory `Map<string, SessionState>` — TS gets cross-invocation sharing through its long-lived daemon process, not from disk. Phase 6A ships a `FileSessionStore implements CommandSessionStore` as a Dart-port-only bridge so users get cross-shell state before Phase 6B (full daemon) lands. Phase 6B will reuse this store inside the daemon. Code (~450 Dart LOC): `runtime/paths.dart` (StatePaths + resolver mirroring TS `daemon/config.ts` file layout: `~/.agent-device/{daemon.json,daemon.lock,daemon.log,sessions/}`), `runtime/file_session_store.dart` (one JSON file per session, per-writer unique `.tmp.<pid>_<hex>` names, `RandomAccessFile.lock(FileLock.exclusive)` on a `.lock` sidecar, atomic rename, corrupt-file tolerance), `cli/base_command.dart` threads session-stored `deviceSerial` into `DeviceSelector` when the user hasn't passed `--serial` / `--device` (so `open` in shell A → `snapshot` in shell B auto-resolves the same device), `--state-dir` + `--ephemeral-session` global flags, `session list/show/clear` subcommands. Bug fixed during validation: `AgentDevice.open` was overwriting the session to `{name, deviceSerial}` on every call — losing the prior `appId`. Now merges via `copyWith`. Tests: 11 unit tests (FileSessionStore, including 8-isolate concurrent write — caught a real race on a shared `.tmp` path) + 6 live cross-invocation tests spawning separate `dart run bin/agent_device.dart` subprocesses and asserting state survives (open→show, snapshot-without-platform, merge-not-clobber, clear, ephemeral, multi-session-name). Totals: 531 unit + 5 AgentDevice live + 23 CLI blackbox live + 6 cross-invocation live = 565. dart analyze clean. Phase 6B (full daemon process + HTTP RPC + lockfile singleton + auto-spawn) is still scoped but deferred until iOS (Phase 8) lands — the daemon's main value-add is amortizing expensive long-lived state like the iOS XCUITest runner, which doesn't exist yet.
- **2026-04-23**: Phase 5 MVP done — working `agent-device` CLI. Commit `9d9ab78` (~800 Dart LOC under `lib/src/cli/`, 18 subcommands). Architecture: each CLI command is a typed `Command` subclass of `AgentDeviceCommand` (which in turn extends `args/command_runner.dart::Command<int>`) that parses its own positionals/flags and drives `AgentDevice`. Shared flags (`--session`, `--platform`, `--serial`, `--device`, `--json`, `--verbose` / `--debug`) live on `AgentDeviceCommand`; global flags at the top-level parser are honored too (either `agent-device --json snapshot` or `agent-device snapshot --json` works). JSON envelope: `{"success": true, "data": ...}` or `{"success": false, "error": {code, message, hint, diagnosticId, logPath, details}}` — matches the TS Node CLI shape. Commands wired: `devices`, `snapshot`, `screenshot`, `open`, `close`, `tap`, `fill`, `type`, `focus`, `back`, `home`, `app-switcher`, `swipe`, `scroll`, `longpress`, `appstate`, `apps`, `clipboard`. Exit codes: 0 ok, 1 error, 64 usage. Validated end-to-end on the Pixel 9 Pro emulator — `devices`/`open`/`snapshot`/`appstate`/`home`/`screenshot` all round-trip correctly in both human and JSON modes; a 179 KB PNG was captured via the CLI. `package:args` replaces the 2100-LOC TS `command-schema.ts` / `args.ts` / `cli-option-*.ts` / `cli-options.ts` chain per the Phase 1 decision. **Deferred** (not blocking for daily use): `batch` command (runs multiple commands in one JSON payload — needs `core/batch.ts` port), `replay` / `test` commands (run `.ad` scripts — Phase 10), `record` / `trace` / `logs` / `network` / `perf` / `diff` observability commands (Phase 10), interactive selector targeting (`agent-device press @e3` style — needs interactor registry from Phase 4 deferred items), `alert`, `rotate`, `pinch`, `press` (the TS "keypress" command). 520 unit tests + 5 live tests passing; analyze clean.
- **2026-04-23**: Phase 4 follow-ups landed (`4729bee`) from verifier audit: `CommandSessionRecord.copyWith` couldn't clear nullable fields — added `clearFields` sentinel set. `closeApp` now actually clears `appId` from session state. `SnapshotState` added to public barrel. `CommandPolicy.maxImagePixels` corrected from fabricated 50M/25M to TS-matching 20M/20M. Removed a spurious `sessions.get` in `getClipboard`. Added Phase-5-required `AgentDevice` wrappers: `pinch`, `pressKey`, `readText`, `findText`, `handleAlert`, `pushFile`, `openSettings`. 516 unit tests.
- **2026-04-23**: Phase 4 done — programmatic runtime façade. Single commit `0df6bbc` (~700 Dart LOC, 15 new unit tests). Shape diverges from the TS `bindCommands` dynamic-dispatch pattern: Dart gets a typed `AgentDevice` class with concrete methods (`open`, `snapshot`, `openApp`, `tap`, `fill`, etc.) that populate `BackendCommandContext.deviceSerial` from session state on every call. Includes `MemorySessionStore`, `CommandSessionRecord` / `CommandSessionStore` / `CommandPolicy` / `CommandClock` / `DiagnosticsSink` types, minimal device resolution (pick first matching via `Backend.listDevices`). Session state mutates on `openApp`/`closeApp`; `closeApp` falls back to the session-stored `appId`. Deferred to Phase 5: full `core/dispatch.ts` (906 LOC — tangles CLI flag resolution with dispatch), `core/batch.ts`, `core/interactors.ts`, `commands/index.ts` catalog. Validation: the live test (`test/platforms/android/android_live_test.dart`) was rewritten to drive `AgentDevice` instead of raw `AndroidBackend`; all 5 tests pass end-to-end on the Pixel 9 Pro emulator — session open, snapshot (51 nodes), screenshot (179 KB PNG), getAppState, listApps, pressHome. 513 unit tests + 5 live tests passing; analyze clean. **This is the "can run simple programmatic tests" milestone** — an SDK consumer can now `import 'package:agent_device/agent_device.dart'` and drive an Android device with ~10 lines of Dart.
- **2026-04-23**: Phase 3 done. exec gap filled — `runCmdDetached` / `runCmdBackground` now return `Future<Process>` / `Future<ExecBackgroundResult>` (Dart async, not TS sync). Backend abstraction (`lib/src/backend/`, 8 files) + cross-platform helpers (`lib/src/core/`, `lib/src/platforms/`) + Android platform (16 files in `lib/src/platforms/android/`) ported across Wave A (foundations), Wave B (snapshot layer), Wave C1 (helpers + light actions), Wave C2 (input_actions + settings), Wave C3 (app_lifecycle + AndroidBackend assembly) — ~7,800 Dart LOC under `lib/src/`. Key architectural decision (**option 3**): `Backend` is `abstract class` with default-throws implementations of every method; only `platform` is abstract. Subclasses override what they support. Mirrors TS's structural `AgentDeviceBackend` type where consumers construct partial objects. `BackendCommandContext` gains a Dart-port-only `deviceSerial` field (Phase 4 runtime populates). `AndroidBackend` wires 25 methods (snapshot/screenshot, tap/fill/typeText/focus/longPress/swipe/scroll, pressBack/pressHome/rotate/openAppSwitcher, clipboard, setKeyboard, openApp/closeApp/getAppState/listApps, triggerAppEvent, listDevices/bootDevice, installApp/reinstallApp); the rest inherit `unsupported`. Wave C3 ran into token-budget issues once and a design issue once; the second attempt + hand-wiring resolved both. feature-port-verifier audited the full phase (`1c035f4..73299f0`) and caught five missed wirings that would have broken Phase 4 command dispatch — all five fixed before closing the phase. 498 tests passing; `dart analyze` clean; zero `dynamic` under `lib/src/`. **Known gaps for Phase 4**: Wave C Android functions take `String serial` instead of TS `DeviceInfo`, so `device.target` (TV vs mobile) branching was dropped (`_resolveAndroidLaunchCategories` always returns mobile — TODO in `app_lifecycle.dart:155`); `.aab` install (`_installAndroidAppBundle`) stubbed as `UnimplementedError` pending bundletool port; Wave C unit tests are shallow (construct-call-only) — deferred to a `withMockedAdb` helper in Phase 4. `commands/index.ts` catalog deferred to Phase 4 where runtime types land.

- **2026-04-23**: Phase 8B done — iOS XCUITest runner bridge live on simulator. Commit `41d6724`. The Swift `ios-runner/` project was copied verbatim from upstream `agent-device/ios-runner/` (signing pinned to the user's Apple Developer account / bundle ids — `dev.roszkowski.agentdevice.runner` / `.runner.uitests`) and built once via `xcodebuild build-for-testing -derivedDataPath ios-runner/build`. Dart-side bridge (`lib/src/platforms/ios/runner_client.dart`, ~350 LOC) resolves the built products dir (env `AGENT_DEVICE_IOS_RUNNER_BUILD_DIR` → walk-up search for `ios-runner/build/Build/Products/`), finds the `.xctestrun` template, patches it with a free loopback port (plutil xml1 → regex-sub of `__TESTROOT__` → plutil binary1 → `plutil -insert TestConfigurations.0.TestTargets.0.EnvironmentVariables.AGENT_DEVICE_RUNNER_PORT`), and launches `xcodebuild test-without-building` detached via `sh -c 'exec xcodebuild … > logPath 2>&1'`. Polls the TCP listener every 500ms up to 60s. `IosRunnerClient.send` POSTs JSON to `http://127.0.0.1:<port>/command` and decodes `{ok, data|error}`; `stop` is best-effort `{command:'shutdown'}` + SIGKILL on the xcodebuild pid. `IosBackend` now wires snapshot / tap / longPress / swipe / typeText / pressHome / pressBack / openAppSwitcher / rotate through `_runner(ctx)` which resolves in order: in-process module cache → disk record at `~/.agent-device/ios-runners/<udid>.json` → fresh launch. Cross-invocation disk cache is essential — a simulator only hosts one XCUITest session at a time, so two back-to-back CLI invocations must reuse one runner. `captureScreenshot` stays on simctl (reliable, no runner dependency). Four bugs surfaced during validation and fixed in-phase: (1) `plutil -insert` path needs `TestConfigurations.0.TestTargets.0.EnvironmentVariables.…` not the bundle id prefix; (2) moving the xctestrun to `/tmp` broke relative `__TESTROOT__` paths — resolved by substituting the placeholder to the build products dir before plutil-converting back to binary; (3) the Dart CLI hung 30+s after emitting JSON (HttpClient keepalive timers keeping the VM alive past idle) — fixed with explicit `exit(code)` in `bin/agent_device.dart` after stream flushes; (4) back-to-back `snapshot` invocations raced competing xcodebuilds on the sim until the disk-cache path landed. Wire-format corrections: swipe sends `{x,y,x2,y2}` (not `fromX/fromY/toX/toY`); rotate uses kebab-case orientation tokens (`landscape-left`, not `landscapeLeft`). `RotateCommand` was named in help text but never registered — added and wired in `run_cli.dart`. 6 new iOS runner live tests (`ios_runner_live_test.dart`) cover cold-start (<90s), warm reuse (<20s), home/tap/app-switcher, swipe field-name regression, rotate round-trip, and the on-disk runner record layout; 5 Phase 8A tests updated (snapshot now works; pinch still returns UNSUPPORTED_OPERATION because the runner has no multi-touch handler). Totals: 542 unit + full iOS suite green on iPhone 17 / iOS 26.2 simulator; `dart analyze` clean. Deferred: `ensureSimulator`, `devicectl` for physical-device install/launch, pinch multi-touch on the Swift side — revisit when Phase 9 (macOS helper) or a physical-device ticket lands.

- **2026-04-24**: Phase 8C done — iOS polish. Commit `0ba523c`. Closes the remaining Phase 8 surface-area gaps identified in a post-8B audit (pinch multi-touch, clipboard, ensure-simulator, physical-device enumeration + launch/terminate via `devicectl`). `IosBackend.pinch` forwards `{scale, x?, y?}` to the runner's existing `.pinch` handler (unused until now). `getClipboard` / `setClipboard` shell out to `xcrun simctl pbpaste|pbcopy <udid>` — simulator only; physical-device clipboard stays unsupported (Apple does not expose it). New `ensure_simulator.dart` + `EnsureSimulatorCommand` resolve a simulator by name (default: reuse-existing) and optionally boot it; `--create-new` / `--no-boot` flags mirror TS `ensureSimulatorExists`. New `devicectl.dart` ports the slice of `agent-device/src/platforms/ios/devicectl.ts` the port needs today: `runIosDevicectl`, `listIosDeviceApps`, `launchIosDeviceProcess`, `terminateIosDeviceProcess`, `listApplePhysicalDevicesViaDevicectl`. `listAppleSimulators` gets a sibling `listAppleDevices` that merges simulators + physical; `IosBackend.listDevices` switches over. `IosBackend.openApp` / `closeApp` / `listApps` resolve kind (`simulator` | `device`) per UDID (cached in `_IosKindCache`) and dispatch to the right tool. `.ipa` install/uninstall/reinstall is still deferred — the install-artifact chain (archive prep, bundleId extraction, app icon resolution) is a larger port and physical-device install needs live-device validation anyway. 3 new devicectl payload-parser unit tests; 2 new runner live tests (pinch round-trip, clipboard set→read round-trip); new `ios_ensure_simulator_live_test.dart` covering the reuse-existing path. Stale "pinch returns UNSUPPORTED_OPERATION" description in `ios_live_test.dart` fixed (test was already probing selector-press leakage; only the description was out of date). Stale Phase-8A-ish comment in `base_command.dart` replaced. Totals: 545 unit + 8 iOS runner live + 6 iOS CLI live = 559 iOS-reachable tests; `dart analyze` clean. Phase 8 closes here. Phase 9 (macOS / Linux) skipped for now — user's ask is to go straight to Phase 10 replay runner + observability next.

- **2026-04-24**: Phase 10 MVP opened — `.ad` replay runner + `test` CLI. Commit `77c776e`. New `lib/src/replay/replay_runtime.dart` + `lib/src/cli/commands/replay_cmd.dart`. The replay runtime `runReplayScript(scriptPath, device, {artifactDir, onStep})` reuses the Phase 1 `parseReplayScript` and drives a small dispatch table that maps each `SessionAction` to a concrete `AgentDevice` method (open/close/home/back/app-switcher/rotate/type/swipe/longpress/pinch/click-press-tap/fill/snapshot/screenshot). Per-step results carry `{ok, errorCode, errorMessage, durationMs, artifactPaths}`. Default is fail-fast (TS `replayUpdate=false`); healing (`session-replay-heal`) is deferred. Two new CLI commands — `agent-device replay <script.ad>` (JSON envelope or per-step human progress) and `agent-device test <paths-or-glob> [--retries N]` (accepts a file, a directory, or a minimal glob — `**/*.ad` / `*.ad`). Per-test artifacts land under `<state-dir>/test-artifacts/<script-name>/<timestamp>/`. Validated end-to-end on iPhone 17 simulator: a 2-step `home → snapshot` script round-trips via the replay command and via the test command. 5 new unit tests use a `RecordingBackend` that captures every `Backend` method invocation — basic flow, swipe coord parse, fail-stop, unknown-command → `UNSUPPORTED_OPERATION`, JSON-payload rejection. 2 new iOS live tests. Totals: 550 unit + 5 replay unit + 2 iOS replay live = 557; `dart analyze` clean. Deferred inside Phase 10 (next commits): healing, record-trace + video recording, app-log / network-log, per-test context metadata, runtime-hint session bootstrap (React-Native metro).

- **2026-04-24**: Phase 10 self-healing replay landed. Commit `24edcbd`. Port of `session-replay-heal.ts` + the runtime wiring that consumes it. `lib/src/replay/heal.dart` (~210 LOC) exposes `healReplayAction(action, nodes, platform)` which collects candidate selector expressions from an action (prefers `action.result.selectorChain`, falls back to reconstructing from positionals — selector-form click/press/fill/get, or the selector portion of `is` / `wait`), resolves each against the supplied snapshot via the Phase 7 `resolveSelectorChain`, and rewrites the action using `buildSelectorChainForNode`. `runReplayScript` gained a `replayUpdate` flag: on a selector-backed step failure it takes a fresh `AgentDevice.snapshot()`, heals, and retries the step once. On success the in-memory action list is patched and, when any heal succeeded, the entire script is serialized back to the source `.ad` file with `serializeReplayScript(actions, contextLine: …)` so the leading `context ...` header is preserved. CLI: `--replay-update` on both `agent-device replay` and `agent-device test`. When absent, the runner is still fail-fast (TS parity). The result envelope gains `healed: <int>` and `rewritten: <bool>` fields plus per-step `healed: true` markers. 14 new heal unit tests cover candidate collection across click/fill/is/wait/get (both @ref and recorded-chain sourcing), `inferFillText`'s four positional layouts, and end-to-end rewriting including the "non-selector commands are not healed" guard. 2 new replay-runtime tests exercise the full flow via a `_HealBackend` that fails the first tap and returns a matching snapshot — asserts `healed=1`, `rewritten=true`, stale selector removed from disk, context line preserved. iOS replay live suite still green on simulator. Totals: 568 unit + 14 iOS live = 582 reachable; `dart analyze` clean. Still deferred in Phase 10: record-trace + video capture, app-log / network-log observability, runtime-hint / metro session bootstrap.

- **2026-04-24**: Phase 10 minor — `appstate` action wired in the replay runner (`67e3138`). Read-only foreground probe useful as an assertion step mid-script.
- **2026-04-24**: Phase 10 record-trace — iOS simulator video capture. Commits `9088840`. `IosBackend.startRecording`/`stopRecording` drive the Swift runner's existing `recordStart`/`recordStop` handlers. `appBundleId` comes from the session's `openApp` via `ctx.appBundleId`/`appId`. On stop, we scan the runner's log file for `AGENT_DEVICE_RUNNER_RECORD_START ... resolvedOutPath=<abs>` and copy the MP4 from the simulator's sandbox `tmp/` (directly accessible from the host — no `simctl pull` hop needed). Self-heals one "recording already in progress" case on start by issuing a recordStop + retry (TS parity). `AgentDevice.startRecording(outPath, {fps, quality, showTouches})` / `stopRecording(outPath)` wrappers. CLI: `agent-device record start <outPath> [--fps N] [--quality Q]` + `agent-device record stop <outPath>` as nested subcommands. Replay dispatch: `record start <outPath>` / `record stop <outPath>` action forms so `.ad` scripts can fence a segment with a video around it — stop's artifact path is surfaced on the step result. Android recording stays unsupported (needs a cross-invocation PID cache for `adb shell screenrecord` — separate piece of work). Tests: 1 new replay unit test (record start/stop round-trips + artifact path), new `ios_record_live_test.dart` (opens Safari, records home, asserts a >1KB MP4 with valid `ftyp` header — 1.3MB file produced on iPhone 17 sim). Totals: 570 unit + 17 iOS live = 587; `dart analyze` clean.

- **2026-04-24**: Phase 10 observability — app-log capture (iOS+Android) + Android recording. Commits `c6af1e0`, `3710d18`, `2452e6a`. **iOS logs** (`c6af1e0`): `IosBackend.readLogs` shells out to `xcrun simctl spawn <udid> log show --predicate ... --last|--start ...` with the TS-equivalent predicate (subsystem / processImagePath / senderImagePath ENDSWITH[c] bundleId). Accepts relative windows (`30s`/`5m`/`1h`/`2d`) and absolute timestamps. Strips the compact-style header, returns one entry per line. Simulator only — physical devices surface `UNSUPPORTED_OPERATION` pointing at `devicectl device log stream` (streaming TBD). `AgentDevice.readLogs({since, limit})` wrapper, new `agent-device logs [--since ...] [--limit N] [--out <path>]` CLI. **Replay auto-dump** (same commit): on step failure inside `runReplayScript`, if an artifact dir is active, best-effort `device.readLogs(since: '30s')` gets written to `<artifactDir>/step-<n>-logs.txt` and surfaced on the step's artifactPaths. Android capture, no crash from unsupported backends. **Android logs** (`3710d18`): `AndroidBackend.readLogs` via `adb -s <serial> logcat -d -v time -T <ts>`. Dart-side helper `resolveAdbLogcatTimeWindow` converts the iOS-flavoured relative forms to the `MM-DD HH:MM:SS.mmm` shape logcat expects; absolute timestamps pass through unchanged; default 5m. Drops the per-buffer "beginning of" banners. Full logcat — package-filtering deferred (would need `adb shell pidof <pkg>`). 7 new unit tests for the time-window resolver, anchored against a fixed `now` so expectations stay deterministic. **Android recording** (`2452e6a`): `AndroidBackend.startRecording`/`stopRecording` drive `adb -s <serial> shell 'screenrecord <remote> >/dev/null 2>&1 & echo $!'` (TS parity — the device-side fork plus pid echo), persist `{serial, pid, remotePath, outPath}` to `<stateDir>/android-recorders/<serial>.json`, SIGINT the on-device PID on stop so the moov atom flushes, wait ~1.2s for finalization, `adb pull` to the host path, then clean up the on-device file + the disk record. Stale records get SIGINT'd on a subsequent startRecording so we don't leak screenrecord processes. No Android live test (no emulator in the per-run harness); unit suite 578 green, `dart analyze` clean. Pulled validation on iPhone 17 sim: 26 log entries / 4.2KB / 10s window for Safari via the new `logs` command, and a 1.3MB MP4 via `record start/stop`. Deferred in Phase 10: streaming logs (background child + PID file lifecycle), per-package logcat filter, `network-log` regex parser, runtime-hint / metro session bootstrap.

- **2026-04-24**: Phase 10 — network-log parser + `agent-device network` CLI. Commit `a0d6006`. Port of `agent-device/src/daemon/network-log.ts` into `lib/src/diagnostics/network_log.dart` (~440 LOC, pure text work — no live device needed). Reverse-scans an app-log dump (captured via the new `agent-device logs --out <path>`), runs regex + embedded-JSON heuristics per line, and requires at least one explicit network signal (HTTP verb + URL, `method=` field, inline status, or `URL=`/`headers=`/`body=` label) so plain documentation URLs in log messages don't get reported as traffic. Android backend unlocks cross-line enrichment keyed on `packet id <N>` within a ±12-line window so OkHttp/Retrofit interceptor output that spreads request→response across several interleaved lines still collapses into a single complete entry (with timestamp / status / durationMs backfilled). Apple backends stay strictly per-line. `mergeNetworkDumps` dedups on `timestamp|method|url|status|raw` for callers that want to concat multiple windows. Limits clamp to TS-matching defaults (maxEntries 25/1-200; maxPayloadChars 2048/64-16384; maxScanLines 4000/100-20000); truncation appends `...<truncated>`. New `agent-device network <logPath>` command with `--backend / --include summary|headers|body|all / --max-entries / --max-payload-chars / --max-scan-lines`; human mode prints one line per entry, `--json` emits the full `NetworkDump.toJson()`. Tests: 7 unit tests, 5 of which are verbatim Dart ports of the TS test cases (iOS JSON-in-line parse, Android GIBSDK enrichment, interleaved packet scan window, Apple-backend-doesn't-enrich, plain doc-URL rejection, missing log file); plus new `mergeNetworkDumps` dedup test. Totals: 585 unit. `dart analyze` clean.

- **2026-04-24**: Phase 10 — `measurePerf` wired on Android + iOS simulator. Commit `acd958b`. Bridges the pre-existing Phase 1 Android perf port (`dumpsys cpuinfo` / `meminfo`) into `AndroidBackend.measurePerf` — the module was done but the backend method still threw `unsupported`. Adds `lib/src/platforms/ios/perf.dart` (~240 LOC) — resolves `CFBundleExecutable` via `plutil -extract CFBundleExecutable raw -o -`, runs `/bin/ps -axo pid=,%cpu=,rss=,command=` inside the simulator via `simctl spawn` (absolute `/bin/ps` required — bare `ps` returns ENOENT inside the sim's spawn PATH), filters matched processes, aggregates. Bug found during live validation: the TS `readProcessCommandToken` splits on whitespace which breaks on simruntime paths containing spaces (`/.../iOS 26.2.simruntime/...`). The Dart port uses a path-suffix matcher instead (`endsWith('/<exe>')` or `contains('/<exe> ')`) so the space-bearing Safari command line from `ps` actually matches. `AgentDevice.measurePerf({metrics, sampleMs})` + new `agent-device perf [--metric cpu|memory]` CLI. Physical-device iOS still returns `UNSUPPORTED_OPERATION` — `xctrace activity-monitor-process-live` + XML trace export is ~500 LOC of separate heuristics, deferred. Tests: 8 parser unit tests including the space-path regression and an explicit "don't false-match on argv substring" case; new `ios_perf_live_test.dart` (2 cases) asserts Safari's cpu/memory samples on the booted simulator. Live run: Safari 123 MB RSS. Totals: 593 unit; `dart analyze` clean.

- **2026-04-24**: Phase 10 — streaming log capture (iOS sim + Android) with cross-invocation PID cache. Commit `886a6a6`. Mirrors the screenrecord pattern: new `Backend.startLogStream` / `stopLogStream` with a shared on-disk record at `<stateDir>/log-streams/<deviceId>.json`. Either shell can start, either can stop. iOS uses a detached `sh -c 'exec xcrun simctl spawn <udid> log stream --predicate … > <outPath> 2>&1'` so redirection happens inside the child; Android uses the same shape with `adb -s <serial> logcat -v time [--pid <pidof(pkg)>]`. Android looks up the app PID via `adb shell pidof <pkg>` when a session app is set so logcat narrows to that app only; falls back to the full stream otherwise. Stop SIGINT's the host pid (so logcat and simctl flush buffers cleanly), waits 400ms, reports the final file size; stale records surface as `stale: true` on the result. `AgentDevice.startLogStream(outPath, {appBundleId})` / `stopLogStream()` wrappers. CLI: `agent-device logs --stream --out <path>` / `logs --stop` — mutually exclusive with --stop, --stream requires --out, default one-shot dump behaviour unchanged. Tests: 3 unit tests for `LogStreamRecord` round-trip + malformed rejection; new `ios_log_stream_live_test.dart` (3 cases) asserts a 3s Safari stream yields real bytes on disk, that `--stop` without an active stream is COMMAND_FAILED, and that `--stream` without `--out` is INVALID_ARGS. Live run: 273 B of Safari log captured in 3s on iPhone 17 sim. Android streaming isn't live-validated (no emulator in the per-run harness) but follows the identical pattern. Totals: 596 unit; `dart analyze` clean.

- **2026-04-24**: Phase 10 — iOS physical-device log streaming via `idevicesyslog`. Commit `4a15099`. Current Xcode's `devicectl` has no `log stream` subcommand (the `device` subcommand list is `copy / info / install / notification / orientation / process / reboot / sysdiagnose / uninstall` on the tested Xcode), so the physical-device branch in `IosBackend.startLogStream` now falls back to libimobiledevice's `idevicesyslog -u <UDID>`. Probes `which idevicesyslog` up front; if absent, raises `TOOL_MISSING` with a `brew install libimobiledevice` hint. Spawns the binary detached with stdout → `<outPath>`, persists the host PID through the same `<stateDir>/log-streams/<udid>.json` record as simulator captures. No predicate filter — idevicesyslog emits the full device syslog, callers grep post-hoc. `stopLogStream` re-derives the `backend` label (was hardcoded to the simulator variant) from the resolved device kind so the result distinguishes `ios-device-log-stream-idevicesyslog` from `ios-simulator-log-stream`. Live validation on a paired iPhone 12 mini: 2.2 MB of syslog captured in a 4-second window.
- **2026-04-24**: Phase 10 — iOS physical-device perf via `xctrace activity-monitor`. Commit `2c69053`. Records a 1-second all-processes trace with `xcrun xctrace record --template 'Activity Monitor' --time-limit 1s`, exports the `activity-monitor-process-live` table via `xctrace export --xpath`, parses the XML dump with `package:xml`. New `IosDeviceProcessSample` / `parseIosDevicePerfXml` / `sampleIosDevicePerfSnapshot` / `sampleIosDevicePerfMetrics` in `lib/src/platforms/ios/perf.dart`. Schema walk resolves `ref="N"` indirection so rows sharing process identity with an earlier row still produce complete entries; `<sentinel />` cells collapse to null. `IosBackend.measurePerf` physical-device branch matches rows whose process-name `fmt` contains the bundle id's last segment (typical CFBundleExecutable form), aggregates across matched processes. **CPU is reported as `cpu.lifetime` in seconds, not percent** — `cpu-total` is cumulative lifetime CPU time, so one snapshot can't compute %. Memory remains `memory.resident` in kB from `memory-real`. Tests: 3 new XML-parser unit tests backed by a real 422 KB fixture (1s capture from the paired iPhone 12 mini); new `ios_device_perf_live_test.dart` gated on `AGENT_DEVICE_IOS_LIVE=1` + `AGENT_DEVICE_IOS_DEVICE_UDID=<udid>` (tag `ios-device-live` so the simulator suites can run without it). Live run against Safari on the iPhone: 21.5 s lifetime CPU, 167 MB RSS, ~13 s end-to-end. Totals: 599 unit; `dart analyze` clean. Phase 10 is effectively closed — remaining items are `.ipa` install (bigger, needs install-artifact chain), Android pinch (runner-Swift gap not a Dart one), iOS video recording on physical devices, and a multi-snapshot CPU% delta.

- **2026-04-25**: Phase 10 follow-up — iOS physical-device screen recording via the XCUITest runner over the CoreDevice IPv6 tunnel. The recording flow itself was already wired in Phase 10 (`IosBackend.startRecording`/`stopRecording` → runner → `devicectl device copy from`); the blocker was transport. NWListener inside the UI test sandbox gets wrapped by NECP (`SO_NECP_LISTENUUID failed` in the runner log), which hides the bound port from any external transport — including the CoreDevice tunnel. Two-part fix: (1) **Runner side** — new `RunnerBSDSocketServer.swift`, an HTTP/1.1 server backed by raw POSIX `socket(2)/bind(2)/listen(2)/accept(2)` calls. Bypasses NECP entirely. Binds on `AF_INET6 / in6addr_any` with `IPV6_V6ONLY=0` for a dual-stack listener so the tunnel (which routes from a non-loopback IPv6 remote) can reach it. `RunnerTests.testCommand` switches via `#if targetEnvironment(simulator) || os(macOS)` between the existing `NWListener` path (sim/macOS — those don't sandbox the listener) and the new BSD path (device). (2) **Host side** — `runner_client.dart` reverts from the iproxy/usbmux experiment back to the CoreDevice tunnel. iproxy turned out to be a dead end on iOS 17+ because Apple's remoted/CoreDevice no longer publishes a pairing record into lockdown's keystore (`idevicepair pair` returns "No device found"). The Dart side now resolves `xcrun devicectl device info details → connectionProperties.tunnelIPAddress` once at launch, caches it on the session, and POSTs to `http://[<tunnelIp>]:<port>/command`. Drops the iproxy spawn + cleanup, swaps `iproxyPid` for `tunnelIp` on the session record. Also: device `xcodebuild test-without-building` runs with `-enableCodeCoverage NO` because iOS 17+ refuses to create the runtime-profile directory inside the runner's sandbox (surfaces as `Failed to create directory ... runtime profiles → TEST EXECUTE FAILED`). HTTP requests pin `Connection: close` + `persistentConnection = false` since the tunnel can wedge on lingering keep-alive. Validated end-to-end on the paired iPhone 12 mini (UDID `00008101-0001510C3CF0001E`, iOS 26.x): runner came up in ~14s, `record start` returned 200 in 2s, `record stop` pulled a 1.6 MB MP4 in 453 ms via `devicectl device copy from --domain-type appDataContainer`. README feature matrix already showed ✅ for physical-iOS recording; only the Phase 10 follow-ups roadmap entry was stale and is now removed.

- **2026-04-25**: Phase 10 follow-ups — close the last two readiness items. (1) **iOS device perf is now a real CPU%** instead of lifetime seconds. The TS upstream always takes two consecutive `xctrace record --time-limit 1s` captures and diffs `cpu-total` per pid; the Dart port had taken the shortcut of one snapshot + lifetime seconds with a TODO. New `computeIosDevicePerfDelta(firstSamples, secondSamples, firstCapturedAt, secondCapturedAt, processMatcher, matcherLabel)` is a pure aggregator (testable without xctrace): per-pid Δns / wall-window-ns × 100, summed across matched pids. Pids that appear in the second snapshot without a baseline contribute zero CPU but full memory (lifetime is meaningless without a prior reading). Memory is the resident-set sum of the second snapshot. Multi-core processes can exceed 100%, matching `top` semantics. `IosBackend.measurePerf` device branch swaps `cpu.lifetime` (seconds) → `cpu` (percent) in the wire format; live test `ios_device_perf_live_test.dart` updated to assert the new shape. Total cost is ~15-20s wall (two ~13s xctrace invocations) — slow but accurate. 4 new unit tests in `perf_xml_test.dart` for the delta aggregator (basic CPU% sum, missing-baseline handling, no-match COMMAND_FAILED, zero-window guard). (2) **Replay fps/quality propagation locked in.** The parser, runtime dispatch, AgentDevice façade, and IosBackend wire-encode were already plumbed end-to-end but no replay-runtime test asserted that `record start … --fps 30 --quality 7` actually reaches `Backend.startRecording` with the correct values. Updated `_RecordingBackend.startRecording` to capture all options and added an explicit assertion. `--hide-touches` parses but isn't honored — the TS upstream implements it as a daemon-side ffmpeg overlay driven by the runner's gesture-event capture, which is a separate (larger) port; left in roadmap. Totals: 609 unit; `dart analyze` clean (3 pre-existing lints in fixture-app live tests, unrelated). README feature matrix updated: physical-iOS perf row now reads "xctrace 2× 1s + delta — true CPU%".

- **2026-04-25**: Phase 10 follow-ups — iOS app install / uninstall / reinstall (path sources only). New `lib/src/platforms/ios/install_artifact.dart` ports the path-source slice of `agent-device/src/platforms/ios/install-artifact.ts`: accepts a `.app` directory or `.ipa` archive, unzips `.ipa` into a tmpdir, walks `Payload/` to enumerate bundles, reads `CFBundleIdentifier`/`CFBundleDisplayName`/`CFBundleName` via `plutil -extract`, and resolves single-app archives directly + multi-app archives via `appIdentifierHint` (matches by exact bundle name, then by bundle id when the hint contains a dot). Cleanup callback removes the tmpdir on success/failure. URL sources, archive extraction (`.zip`/`.tar.gz` around the `.app`/`.ipa`), and EAS/GitHub Actions trust-list logic deferred until needed. New `installIosDeviceApp(udid, installablePath)` + `uninstallIosDeviceApp(udid, bundleId)` helpers in `devicectl.dart` (uninstall is tolerant of "not installed" — returns false rather than throwing). `IosBackend.installApp`/`uninstallApp`/`reinstallApp` overrides auto-detect simulator-vs-device and dispatch to `simctl install`/`uninstall` or the devicectl helpers; install result surfaces `bundleId` + `appName` so downstream `open` calls don't need a separate lookup. New `Backend.uninstallApp` abstract method, mirrored on `AgentDevice` façade and `AndroidBackend` (the Android `uninstallAndroidApp` was already implemented under a private name; un-priv'd to expose it). New CLI commands `agent-device install <path> [--app <bundleId>]`, `agent-device uninstall <bundleId>`, `agent-device reinstall <bundleId> <path>`. 8 new unit tests in `install_artifact_test.dart` build real `.app` + `.ipa` fixtures in tmpdirs (plutil-authored Info.plist + `zip -r`) and walk every branch — single-app, multi-app with bundle-id hint, multi-app with bundle-name hint, multi-app missing hint, empty Payload, unknown extension, missing path. Live-validated on the paired iPhone 12 mini: install of `AgentDeviceRunner.app` (`xcrun devicectl device install app`) succeeds, uninstall + reinstall round-trip clean. Totals: 617 unit; `dart analyze` clean (3 pre-existing lints in fixture-app live tests, unrelated). README feature matrix gains an install/uninstall/reinstall row; Phase 10 follow-ups roadmap loses the `.ipa install` line and gains a "URL sources / nested archives" deferred entry covering the slice still upstream-only.

- **2026-04-25**: Phase 12 polish — `dart compile exe` + shell completions + pub.dev dry-run prep. (1) **Native binary**: new `make compile` Make target → `dist/agent-device` (~7.8 MB self-contained, no Dart-runtime dep on the host) plus a `dist/ad` symlink. `bin/agent_device.dart` now detects the invoked basename and threads it as the `executableName` argument so `--help` shows `Usage: ad …` when invoked through the short symlink. New helper `buildCliRunner({executableName})` in `run_cli.dart` extracts the runner construction so the completion subcommand can introspect the registered subcommand list without circular dependencies. (2) **Shell completions**: new `completion bash|zsh|fish` subcommand emits a static script. Bash uses `complete -F _agent_device_complete` with subcommand list + value-completion for `--platform` (`ios|android|macos|linux|apple`). Zsh uses `#compdef agent-device ad` + `_arguments`. Fish emits per-subcommand `complete -c agent-device -f -n '__fish_use_subcommand' -a '<name>'` lines plus separate `complete -c ad …` mirrors. Pure builder `buildCompletionScript(shell)` exposed for unit tests. 5 new unit tests in `cli/completion_test.dart`. (3) **pub.dev dry-run readiness**: `dart pub publish --dry-run` from `packages/agent_device/` now passes everything except a `LICENSE` file (user decision — README originally said "Same as upstream `agent-device`" but upstream license isn't on hand). Added `homepage`/`repository`/`issue_tracker` to pubspec, package-level `README.md` and `CHANGELOG.md` so pub.dev sees a self-contained intro. `publish_to: none` is left in for now until the LICENSE is decided. Totals: 622 unit; `dart analyze` clean (3 pre-existing fixture-app lints, unrelated). Remaining Phase 12 item: byte-for-byte CLI output diff against the Node CLI on a `.ad` corpus — infrastructure-heavy and easier to do once a stable corpus exists.

- **2026-04-25**: Upstream-drift catch-up — port the TS parametrise-`.ad` feature (commit `2062bffc`). New `lib/src/replay/replay_vars.dart` ports the variable scope: `${VAR}` substitution with `${VAR:-default}` fallback and `\${...}` escape, source merging across CLI `-e`, `AD_VAR_*` shell env, file-local `env KEY=VAL` directives, and trusted runtime built-ins (`AD_PLATFORM`/`AD_SESSION`/`AD_FILENAME`/`AD_DEVICE`/`AD_ARTIFACTS`). Precedence is high → low: CLI > shell > file > builtins. The `AD_*` namespace is reserved — every untrusted layer rejects it (closes the `AD_VAR_AD_SESSION=evil` shadowing vector). Stripped shell keys that re-enter `AD_*` (e.g. `AD_VAR_AD_SESSION` would land back as `AD_SESSION`) are silently dropped before merging. `script.dart` extends `ReplayScriptMetadata` with an `env` map, adds `parseReplayScriptDetailed` that returns 1-based action lines for diagnostics, and rejects `env` directives that appear after an action. Quoted env values (`env KEY="hello world"`) are JSON-decoded so users can carry whitespace/escapes without shell quoting. `runReplayScript` builds the scope at start, runs `resolveReplayAction(action, scope, file:line)` before each dispatch so positionals/flags/runtime-hint strings see fully substituted text, and guards `replay -u` against rewriting scripts that still carry env directives or `${...}` interpolation tokens (the writer can't yet round-trip those, so failing loud beats silently dropping them). CLI: new `-e` / `--env KEY=VALUE` (repeatable) on `replay` and `test`; `runReplayScript` accepts an explicit `shellEnv` map (defaults to `Platform.environment`) so tests can pin behaviour. 20 unit tests in `replay_vars_test.dart` cover scope merging, namespace reservation, shell-prefix migration, CLI parsing, interpolation paths, runtime-hint resolution, and the `actionsContainInterpolation` guard. 8 new tests in `script_parser_test.dart` cover env directive parsing + ordering. 3 new integration tests in `replay_runtime_test.dart` exercise CLI-override-vs-file precedence, file:line on unresolved variables, `replay -u` env-rejection, and `AD_VAR_*` shell-env import. Totals: 652 unit; `dart analyze` clean. README replay section gains a "Parameters" subsection covering precedence, fallback/escape syntax, built-ins list, and the `replay -u` limitation.

- **2026-04-25**: Upstream-drift catch-up — TS commits `57ba479e` (screenshot `--max-size` flag) and `bb245675` (Android snapshot freshness). The replay-script parser, replay runtime serializer, and `BackendScreenshotOptions` already had `screenshotFullscreen` / `screenshotMaxSize` plumbing on the .ad side; this commit lights up the CLI surface to match. **Screenshot CLI flags**: `agent-device screenshot <path> [--fullscreen] [--max-size N]`. `BackendScreenshotOptions` gains a `maxSize` field. `AgentDevice.screenshot(outPath, fullscreen, maxSize)` now post-processes the captured PNG via `lib/src/utils/png.dart::resizePngFileToMaxSize` (the box-filter port of TS's pngjs-based resize, already shipped with 5 unit tests) when `maxSize > 0`. Replay dispatch threads `screenshotFullscreen` / `screenshotMaxSize` flags through to the same path so `screenshot /tmp/x.png --max-size 800` in a `.ad` file does the right thing. **Android snapshot freshness**: spot-check confirms the portable platform-layer slice is already in (`packages/agent_device/lib/src/platforms/android/snapshot.dart` has the 8s dump timeout, kill-detection helper, and the actionable "disable animations" hint; `settings.dart` has the `animations` setting wired through `window_animation_scale`/`transition_animation_scale`/`animator_duration_scale`). The remaining TS-side work is daemon-shaped: per-session snapshot freshness baselines and ref-refresh-after-touch logic threaded through `interaction-touch.ts` / `snapshot-capture.ts`. That's coupled to TS's `SessionState['snapshot']` cache which Dart doesn't have — porting it would mean inventing the session-snapshot-cache concept first, deferred until there's a concrete user-impacting bug. Totals: 652 unit; `dart analyze` clean.

- **2026-04-27**: Upstream commit `77365ab7` ("refactor: simplify Android hidden-content hints") ported. Dart commit `0ca18ab`. Changes: (1) `RawSnapshotNode.hiddenContentAbove` / `hiddenContentBelow` made non-final so hint values can be written after construction (TS mutates plain object properties; Dart achieves the same via non-final fields). `SnapshotNode` constructor loses `const` as a consequence. All test files updated to drop `const` from `RawSnapshotNode`/`SnapshotNode` constructor calls. (2) `_applyHiddenContentHintsToNodes` — no-op stub replaced with a real loop that writes hint flags onto nodes by index. (3) `_applyHiddenContentHintsToInteractiveNodes` — no-op stub replaced with a source-node-identity bridge: builds a `Map<AndroidUiHierarchy, RawSnapshotNode>` from the interactive snapshot, then looks up each hinted full-snapshot source node through that map and writes flags — matches the TS approach. (4) `deriveMobileSnapshotHiddenContentHints` fallback path in `snapshotAndroid` (previously commented out with a Wave C TODO) is now live. (5) New `lib/src/utils/mobile_snapshot_semantics.dart` — full port of `src/utils/mobile-snapshot-semantics.ts` including `buildMobileSnapshotPresentation`, `deriveMobileSnapshotHiddenContentHints`, and internal `analyzeMobileSnapshotVisibility`. Also inlines a minimal port of `inferVerticalScrollIndicatorDirections` from `scroll-indicator.ts` (that file not yet ported standalone; the needed subset is sufficient and marked with a comment). Deviation: `scroll-indicator.ts` is not a standalone Dart file yet; the one function needed by `mobile_snapshot_semantics` is inlined there.

- **2026-04-27**: Upstream commit `d77a9211` ("feat: add Android snapshot helper (#454)") ported. Dart commit `a1f2c02`. New files: `android-snapshot-helper/` Java APK (verbatim copy), `scripts/build-android-snapshot-helper.sh` (verbatim copy), `snapshot_helper_types.dart` (constants + type definitions), `snapshot_helper_capture.dart` (`captureAndroidSnapshotWithHelper`, `parseAndroidSnapshotHelperOutput`, `parseAndroidSnapshotHelperXml`), `snapshot_helper_install.dart` (`ensureAndroidSnapshotHelper`), `snapshot_helper_artifact.dart` (`verifyAndroidSnapshotHelperArtifact`, `parseAndroidSnapshotHelperManifest`, `resolveBundledAndroidSnapshotHelperArtifact`), `snapshot_types.dart` (`AndroidSnapshotBackendMetadata`, `androidSnapshotMaxNodes`). Updated files: `snapshot.dart` (helper-first capture path + `AndroidSnapshotOptions` wrapper + backend metadata return), `android_backend.dart` (wires new options). 15 new unit tests in `snapshot_helper_test.dart`. Deviations: (1) `prepareAndroidSnapshotHelperArtifactFromManifestUrl` (remote APK download) not ported — Dart port uses only bundled local APK via walk-up repo search; (2) `snapshotAndroid` takes `AndroidSnapshotOptions` wrapper instead of bare `SnapshotOptions` to accommodate the new helper fields; (3) `_parseInstrumentationRecords` returns a Dart record tuple rather than a named object.

(Append dated entries below as phases land.)
